# NGD-Pion — алгоритм в чистом виде

Преемник `natural_gradient_pion.md`. Здесь зафиксировано то, что осталось
после проверки выкладок: все решения приняты, все тождества проверены
численно. Аппроксимаций нет — плотный точный путь `O(d³)`.

Addendum (sketch, CG, Woodbury, msign) в этом документе не участвует.

---

Сжатое изложение метода целиком — в разделе «The algorithm» в
[`README.md`](README.md). Здесь — полная спецификация с обоснованиями.

## 0. Геометрия

Pion обновляет `W ∈ R^{d_out×d_in}` двусторонним поворотом
`W ← R_out · W · R_in`, обе `R` ортогональны. Сингулярные числа `W`
не меняются никогда — двигаются только сингулярные векторы. Обучаемое
содержимое живёт в алгебре Ли `so(d)`, то есть в кососимметричных матрицах.

Следствие: спектр инициализации = спектр финальной модели, навсегда.

---

## 1. Генераторы

```
G_in  = skew(Wᵀ G) = ½(Wᵀ G − Gᵀ W)      (d_in × d_in,  кососимметричная)
G_out = skew(G Wᵀ) = ½(G Wᵀ − W Gᵀ)      (d_out × d_out, кососимметричная)
```

Это в точности риманов градиент по повороту:

```
⟨G, W·X_in⟩ = ⟨G_in, X_in⟩        для любой кососимметричной X_in
⟨G, X_out·W⟩ = ⟨G_out, X_out⟩
```

**Конвенция изменена 2026-08-28.** Раньше здесь стояло `Wᵀ G − Gᵀ W`, то есть
удвоенный риманов градиент, и всякое `eta`, записанное до этой даты, означает
**половину** того, что то же число означает теперь. Половина добавлена, чтобы
совпасть с эталонной реализацией — у них `skew(WᵀG)`, — и чтобы learning rate,
указанный здесь, и learning rate из их статьи были одной и той же величиной.
Читатель будет их сравнивать.

Что сдвинулось вместе с этим, и всё закреплено тестами в `test_direction.py`:

* `natural_gradient` линеен по `G`, а `alpha = quad/curv` несёт множитель в
  обеих половинах, поэтому **удвоение `eta` воспроизводит прежнюю траекторию
  точно** — для всех NGD-вариантов и для `pion_ablated`.
* Предсказанное падение лосса стало `eta·alpha·quad` вместо `½·eta·alpha·quad`.
  Это не косметика: `pred_drop` кормит `rho`, и оставленная половина сделала бы
  каждое `rho` после смены неверным вдвое.
* `pion` с `scaling="rms"` нормирует применяемое обновление, множитель
  поглощается, `eta` не меняется. **Якорь не затронут.**
* `shampoo-pion` масштабно-инвариантен по построению (`P` идёт как ¼,
  `P^{-1/4}` как √2), поэтому его `eta` тоже не меняется.

Для одного сэмпла `G = δxᵀ` генератор — бивектор `G_in = δ' ∧ x`,
где `δ' = Wᵀδ`. Ранг 2. Батч из `B` токенов даёт ранг ≤ `2B`.

---

## 2. Оператор Фишера

Ковариация бивектора при `x ⊥ δ'` даёт оператор на кососимметричных `X`:

```
F(X) = 2 (B X C + C X B)
```

**Решение: `S = E[δδᵀ] := I`.** Это **не** утверждение, что backward-ошибки
изотропны — измеренная `S` сильно анизотропна, см. ниже. Тогда

```
in-сторона:   B = A,  C = S'   = Wᵀ W
out-сторона:  B = I,  C = A'   = W A Wᵀ
```

где `A = E[xxᵀ]` — единственная статистика, которую нужно копить.

`F` самосопряжён и PSD на кососимметричном подпространстве.

---

## 3. Статистика (каждый шаг)

```
A ← β A + (1−β) xxᵀ / n            β = 0.95
```

Только forward-активации. **Backward-хуки не нужны.**
Точность накопления: **fp32 минимум** (bf16 даёт ошибку в 10³–10⁴ раз).

---

## 4. Факторизация (раз в T_fac шагов)

**in-сторона** — конгруэнция пучка `(A, S')` через симметричный корень:

```
A = U Σ Uᵀ                              (eigh)
Σ ← max(Σ, ε · λ_max(A))                ПОЛ на спектр
A^{-1/2} = U Σ^{-1/2} Uᵀ
S' ← пол на спектр S' тем же ε
M   = A^{-1/2} S' A^{-1/2}
M   = Q Λ_in Qᵀ                         (eigh)
P_in = A^{-1/2} Q
```

даёт `P_inᵀ A P_in = I`, `P_inᵀ S' P_in = Λ_in`.

**out-сторона** — при `S = I` конгруэнция не нужна вовсе:

```
A' = W A Wᵀ = Q_out Λ_out Q_outᵀ        (eigh, P_out ортогональна)
```

Cholesky не используется: конгруэнции нужен любой множитель `B = KKᵀ`,
а симметричный корень позволяет явно контролировать малые собственные.

### Когда конгруэнция не нужна вовсе

`W ᵀW` зависит от формы. При semi-orthogonal инициализации:

| форма | `WᵀW` | in-сторона |
|---|---|---|
| `d_out > d_in` (высокая) | `= I` точно | обычный `eigh(A)`, базис ортогонален |
| `d_out = d_in` (квадрат) | `= I` точно | обычный `eigh(A)` |
| `d_out < d_in` (широкая) | проектор ранга `d_out` | нужен `A^{-1/2}` |

Для квадратной ортогональной `W` дополнительно: `A' = W A Wᵀ` — это `A`,
сопряжённая поворотом, поэтому `Λ_out = Λ_in`, а `P_out = W · P_in`.
**Один `eigh` обслуживает обе стороны**, второе разложение не нужно.

В блоке LLaMA (`d = 512`, `d_ff = 1408`):

| матрица | форма | `Λ_in` | `Λ_out` | `A^{-1/2}` | вырожд. сторона |
|---|---|---|---|---|---|
| `wq wk wv wo` | 512×512 квадрат | спектр `A` | спектр `A` | нет | нет |
| `ffn gate`, `ffn up` | 1408×512 высокая | спектр `A` | спектр `A` + нули | нет | **out** |
| `ffn down` | 512×1408 широкая | смесь `A` и `W` | сжатие `A` | **да** | **in** |

Два независимых свойства, которые легко спутать:

- **обращение** нужно только широким: там `WᵀW` — проектор, и конгруэнцию
  приходится анкорить на `A`.
- **вырожденность** есть у всех неквадратных, просто на разных сторонах:
  у широких `WᵀW` имеет ранг `d_out`, у высоких `W A Wᵀ` имеет размер
  `d_out` при ранге `d_in`.

Оба закрываются одним и тем же полом на спектр, см. ниже.

Градиент в мёртвый блок не попадает ни на одной стороне: на in- потому что
`δ' = Wᵀδ ⊥ null(W)`, на out- потому что `G_out = δ ∧ (Wx)` и `Wx ∈ range(W)`.
Измерено на высокой: `‖G̃‖` в блоке `(ноль,ноль)` равна `3.3e-08` против
`6.0e+01` по всей матрице.

**Главное следствие:** для шести матриц из семи `Λ` — это спектр `A`, и
отдельного объекта «оператор Фишера» в реализации не существует вовсе.
Его собственные значения суть `2(λᵢ + λⱼ)` по спектру `A`. Отсюда же следует,
почему демпфер нужен ровно один: и обращение, и знаменатель работают с одним
и тем же спектром, так что достаточно отфлорить его один раз.

### Демпфирование: одна ручка на три спектра

Вырождаться могут три матрицы — `A`, `WᵀW`, `W A Wᵀ` — каждая тогда, когда
данные или форма `W` не дают ей полного ранга. `W` не демпфируется нигде:
она входит только через две последние, а демпфер в самой `W` сломал бы
ортогональность, на которой всё держится.

**Правило одно и применяется к спектру:**

```
λ ← max(λ, ε·λ_max)
```

Пол, а не сдвиг. Пол тождественен для всех собственных значений выше него,
поэтому хорошо определённые направления сохраняют точный масштаб; сдвиг
`M + εI` возмущает и их, платя точностью везде за защиту, нужную только
малому концу. На широкой `W` при `ε = 10⁻⁴` пол даёт `5.8e-9` финального
лосса против `7.7e-7` у сдвига — в 134 раза.

Отфлорив три спектра, больше ничего подпирать не нужно: `A` защищена и как
анкер (`A^{-1/2}`), и как диагонализуемая (`Λ_in`), а знаменатели
`2(Λᵢ + Λⱼ)` положительны автоматически. **Двух параметров `ε_A` и `ε_F`
не существует — есть один `ε`.**

### `ε` — предохранитель, а не гиперпараметр

Без ядра он не нужен вовсе: квадратная `W` при полноранговой `A` сходится
при `ε = 0` до `3.4e-14`. Ядро есть у любой неквадратной `W` — у широких на
in-стороне, у высоких на out- — и там мёртвый блок есть честный `0/0`,
дающий NaN при `ε = 0`.

| `ε` | квадрат | высокая | широкая |
|---|---|---|---|
| `0` | `3.4e-14` | **NaN** | **NaN** |
| `10⁻¹⁰` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁸` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁶` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁴` | `3.4e-14` | `2.1e-08` | `5.8e-09` |
| `10⁻²` | `2.2e-03` | `3.7e-03` | `3.3e-03` |

Плато — четыре порядка. Подбирать нечего: поставить достаточно большим,
чтобы не было NaN, и достаточно малым, чтобы пол не срабатывал на живых
направлениях.

**Но плато измерено в fp64 и в fp32 не переносится.** Нижняя граница пола
задаётся рабочей точностью: ниже машинного эпсилон малый конец пучка
округляется в шум, и пол там ничего не значит. На широком слое при
`cond(A) = 10⁴`, fp32 против fp64:

| `ε` | `10⁻⁸` | `10⁻⁶` | `10⁻⁵` | `10⁻⁴` | `10⁻²` |
|---|---|---|---|---|---|
| ошибка | `2.2e-01` | `5.6e-03` | `5.5e-04` | `7.7e-05` | `2.1e-06` |

Машинный эпсилон fp32 равен `1.2e-7`, поэтому `ε = 10⁻⁸` там просто
бессмыслен. **Дефолт `10⁻⁴`** — он и выше разрешения fp32, и совпадает со
статистическим оптимумом из held-out замера. В fp64 годится всё до `10⁻¹⁰`.

Пол ставится на все спектры, попадающие в знаменатель: `A`, `WᵀW`, `W A Wᵀ`
и **итоговый `Λ` пучка**. Последнее не избыточно — при неквадратной `W` пучок
растягивает `Λ` на больше порядков, чем разрешает рабочий тип, и без пола на
результате малый конец обнуляется в fp32 несмотря на отфлоренные источники.

**Условие:** semi-orthogonal**Условие:** semi-orthogonal инициализация — и **она не выбрана**.

Спектр `W` замораживается Pion навсегда, поэтому инициализация задаёт его
на всё обучение, и ради выразительности спектр берётся нетривиальным. Тогда
`WᵀW ≠ I` нигде, ярлык не срабатывает, и in-сторона всюду идёт через
конгруэнцию. Раздел выше остаётся как описание того, что было бы при
ортогональной инициализации, и как обоснование, почему out-сторона дешёвая
независимо от неё (она дешёвая из-за `S = I`, а не из-за `W`).

Цена этого решения измерена и оказалась мала:

| спектр `W` | `cond(W)` | `cond(Λ)` | ошибка fp32 | `‖X‖₂` |
|---|---|---|---|---|
| все единицы | `1.0` | `1.0e6` | `9.9e-3` | `20.8` |
| равномерно `[0.5, 2]` | `4.0` | `2.2e6` | `9.8e-3` | `14.4` |
| логравн. `[0.25, 4]` | `15.2` | `1.4e7` | `9.0e-3` | `13.8` |
| гауссова | `650` | `9.2e9` | `9.4e-3` | `22.2` |

Обусловленность пучка растёт на три порядка, но **ни точность, ни норма
шага от этого не зависят**: там, где `W` мала, мал и `δ' = Wᵀδ`, так что
экстремальные `Λ` встречаются с экстремально малым числителем. Ошибка fp32
определяется `cond(A)`, а не `cond(W)`.

Остаётся единственная цена — факторизация на in-стороне дороже в `4.3` раза
(`138` мс против `32` при `d = 512`). Амортизированная по `T_fac`, она
близка к проценту.

Отдельно стоит знать: у **квадратной** гауссовой матрицы `cond(W) ≈ d`
(плотность Марченко–Пастура не отделена от нуля при соотношении сторон 1),
а у прямоугольных с соотношением 2.75 — всего около 4. То есть если спектр
всё же захочется контролировать, трогать нужно матрицы внимания, а не FFN.

---

## 5. Натуральный градиент (каждый шаг)

```
Ỹ    = Pᵀ G_skew P
Y_ij = Ỹ_ij / (2(Λ_i + Λ_j))
X    = skew(P Y Pᵀ)
```

Демпфирования здесь нет. `Λ` приходит из уже отфлоренных спектров (§4),
поэтому знаменатели положительны по построению.

## 6. Trust-region

```
quad = ⟨G_in, X_in⟩ + ⟨G_out, X_out⟩
curv = ⟨X_in, F_in(X_in)⟩ + ⟨X_out, F_out(X_out)⟩
α_t  = min(α_max, quad / (curv + ε))      α_max = 1
```

При свежем базисе `α_t ≡ 1` тождественно: `X = F⁺G` даёт `F(X) = G`,
значит `curv = ⟨X, G⟩ = quad`. Это не приближение, а алгебра.

Отклоняется `α_t` только от **устаревания базиса**. Измерено на
достижимой least-squares цели: монотонный спад `1.000 → 0.996 → 0.992
→ 0.987 → 0.983 → 0.979` по шагам после пересчёта, минимум `0.469`
при `T_fac = 50`.

`α_max = 1` делает механизм односторонним: устаревший базис может
только укоротить шаг. При `α_max = 10` наблюдался разгон до `1.23`.

**Это единственный бесплатный сигнал обратной связи по `T_fac`.**

---

## 7. Обновление весов

```
W ← Cayley(−ηα_t X_out) · W · Cayley(−ηα_t X_in)

Cayley(−cX) = (I + c/2 · X)⁻¹ (I − c/2 · X)
```

Минус в шаге, `X` из п.5 без знака. Тогда
`ΔL ≈ −½ η α_t · quad ≤ 0` по лемме п.1.

Cayley точно ортогонален для кососимметричной `X` при любом вещественном
`c`; `I + cX/2` не вырождается никогда (чисто мнимые собственные значения).
Совпадает с `exp` до `z²`, расходится на `z³` — ретракция второго порядка.

**Точность здесь арифметическая, а не только алгебраическая.** На карте
Ampere и новее torch по умолчанию выполняет матричные операции fp32 в TF32 —
десять бит мантиссы, — и это касается солва внутри Cayley. Измерено на RTX PRO
6000 Blackwell: ошибка ортогональности `4.3e-03` против `3.9e-06`, а
относительный дрейф спектра за 200 двусторонних шагов при угле `1e-2` —
**1.0** против `2.6e-04`. То есть с настройками среды по умолчанию
утверждение о сохранении спектра, на котором держится выбор Cayley против
их усечённой экспоненты, не выполняется вовсе.

Поэтому `ngd_pion.linalg.cayley` выключает TF32 сам, не полагаясь на
вызывающего, и то же делает шаг оптимизатора целиком и накопление `A`.
Тесты на CPU этого не видят: там TF32 не существует. Проверяется
`scripts/gpu_smoke.py`, который специально включает TF32 и требует, чтобы
спектр всё равно удержался.

---

## 8. Shampoo на so(n) — альтернативный преконденционер

Секции 2–6 строят преконденционер из **статистики сети**: `A = E[x x^T]`,
`D = E[dd^T]`, и обращают кронекеровское приближение Фишера. Измерения от
2026-08-27 показали, что цена этого приближения — весь недостающий множитель:
`kfac/exact = 0.0128`, то есть точная кривизна вдоль `X` в 30–170 раз больше
той, по которой шаг нормируется, и вывод `eta* = 2` держится только при
`kappa = 1`.

Shampoo (Gupta, Koren, Singer 2018) строит преконденционер **из самих
градиентов**:

    L = sum_t G_t G_t^T,   R = sum_t G_t^T G_t,   шаг = L^{-1/4} G R^{-1/4}

Никакой статистики активаций, никакого предположения `delta ⊥ x`. Перенесённый
на генераторы вращения из §1 — то есть на `so(n)` и `so(m)`, а не на весовое
пространство — он приобретает три свойства, которых у него в общем случае нет.

**1. Факторы совпадают.** Для кососимметричного `G` верно `G^T = -G`, поэтому

    L = G G^T = -G^2 = G^T G = R

точно. Один аккумулятор на сторону вместо двух, один `eigh` вместо двух,
вдвое меньше состояния. `P` формируется как `G G^T`, а не как `-G G`, чтобы
результат был симметричным PSD и под округлением тоже.

**2. Двусторонняя форма — единственная, замкнутая на алгебре.** При
симметричной `P` и кососимметричной `G`

    (P^{-p} G P^{-p})^T = P^{-p} G^T P^{-p} = -P^{-p} G P^{-p}

то есть шаг остаётся генератором вращения, Cayley по-прежнему точно
ортогонален и сингулярные числа `W` по-прежнему заморожены (§7). Односторонний
`P^{-2p} G` кососимметричность теряет — но **не на первом шаге**: там
`P = -G^2` коммутирует с `G`, и форма случайно оказывается кососимметричной.
Реализация, проверенная только на шаге 0, выглядела бы верной и выходила бы из
алгебры начиная со второго. Обе половины закреплены тестами.

**3. Один градиент — и генератор ортогонализуется.** В вещественной форме Шура
`G` блочно-диагонален с блоками `[[0, th], [-th, 0]]`; тогда `P = G G^T` имеет
собственные значения `th^2`, `P^{-1/4}` даёт `th^{-1/2}`, и сэндвич выдаёт
блоки `th^{-1/2} · th · th^{-1/2} = 1` — **все плоскости вращения поворачиваются
на одинаковый угол**. Одношаговый Shampoo на `so(n)` есть в точности
ортогонализация генератора, аналог того, что Muon делает с весовым градиентом;
накопление по `t` интерполирует обратно к сырому градиенту.

### Что это должно чинить

Полнодлинный ран показал разброс угла поворота между слоями 4658×–36496×
внутри одного шага: Фишер блочно-диагонален по слоям, выравнивает кривизну
внутри блока и ничего не говорит о масштабе между блоками, а `eta` — один
скаляр на все. Pion покупает эту калибровку декретом (`scaling="rms"`).

Сэндвич масштабно-инвариантен: при `G -> cG` имеем `P -> c^2 P`,
`P^{-1/4} -> c^{-1/2}`, и `c^{-1/2} · c · c^{-1/2} = 1`. Схлопывается и разброс
плоскостей внутри слоя (следствие 3), и масштаб между слоями — структурно, без
хуков и без `rms`.

### Демпфирование: развилка, которая может это разрушить

Оригинал добавляет `eps I` к аккумулятору. Абсолютный сдвиг не однороден —
`(c^2 P + eps I)^{-1/4}` не масштабируется как `c^{-1/2}` — и потому теряет
ровно то свойство, ради которого метод берётся, сильнее всего в начале, пока
`P` мала и вырождена. Относительный пол `max(lam, eps · lam_max)` из §4
однороден и сохраняет инвариантность точно, и отдельно измерен в 134 раза
точнее сдвига на широком слое.

Поэтому по умолчанию — пол, а `damping="shift"` воспроизводит оригинал. Разница
в один флаг и её надо измерить, а не обсуждать.

### Что отдаётся

Trust-region. `alpha = quad / curv` выведено из точного `X = F^{-1} G`, что и
делает его безразмерным; при другом преконденционере отношение несёт
размерность и множителем не является. Он выключается явно, и `eta` остаётся
единственным, что задаёт длину шага.

**Контроль.** `power = 0` оставляет сырой генератор и точную ретракцию, то есть
в точности ablated Pion, в том же коде и том же харнессе.

## Гиперпараметры

| параметр | значение | статус |
|---|---|---|
| `η` | подбирать | единственный настоящий гиперпараметр |
| `ε` | `10⁻⁴` | предохранитель; нижняя граница задана точностью fp32 |
| `β` (EMA для `A`) | `0.95` | не проверялся |
| `α_max` | `1` | односторонний, обосновано |
| `T_fac` | `100`, фиксированный | адаптивный ломает воспроизводимость |
| точность шага | fp32 | fp64 ничего не даёт и недоступен на части бэкендов |
| точность `A` | fp32 минимум | bf16 даёт ошибку в `10³`–`10⁴` раз |

## Реализация

Модуль на раздел, чтобы код читался против этого документа:

| модуль | раздел | содержимое |
|---|---|---|
| `linalg.py` | — | `skew`, пол на спектр, Cayley |
| `covariance.py` | §3 | накопление `A`, с запретом на низкую точность |
| `factorization.py` | §4 | базисы, оба пути, батчинг по формам |
| `direction.py` | §1, §5, §6 | генераторы, решение, trust-region |
| `optimizer.py` | §7 | оркестрация и ретракция |
| `hooks.py` | — | адаптер под `nn.Linear` |
| `reference.py` | весь | numpy-транскрипция, оракул для тестов |

Оптимизатор принимает **параметры, а не модули**, и ковариация подаётся
снаружи. Это то, что позволяет существовать адаптерам под чужие типы слоёв —
`ColumnParallelLinear` в Megatron, слитый QKV — не трогая сам оптимизатор.

`reference.py` намеренно наивен: он транскрипция этого документа, а не рабочий
код. Torch-путь считается верным ровно настолько, насколько он его
воспроизводит, и это закреплено тестами.

## Решения и почему

| решение | почему |
|---|---|
| `S = I` | использование измеренной `S` делает шаг **хуже**, а не лучше (см. ниже); плюс out-сторона схлопывается в ортогональный `eigh`, cond 9 против 7350, `A` остаётся единственной статистикой и backward-хук не нужен; KL-оптимально при фиксированном факторе (Lin et al. 2026), входная сторона эмпирически лучше градиентной (Eschenhagen et al. 2026) |
| симметричный корень, не Cholesky | конгруэнции хватает любого `B = KKᵀ`; малые собственные контролируются явно |
| маски нет | пол на спектрах делает знаменатели положительными; маска сверх этого не нужна |
| один `ε`, пол на три спектра | пол не трогает живые направления; на широкой `W` в 134 раза точнее сдвига; плато четыре порядка |

| никакого усечения | шаг живёт в малых собственных `A` (усиление `1/√λ`); усечение на `10⁻⁴` = ошибка 100% |
| `α_max = 1` | устаревший базис должен тормозить, а не разгонять |
| fp32 везде | на реальных спектрах сквозная ошибка `2.5e-5…1.7e-3` |
| без momentum | пока; см. открытые вопросы |

---

## Что проверено численно

| утверждение | невязка |
|---|---|
| `F(X) = 2(BXC + CXB)` против Монте-Карло (4M сэмплов) | `1.5e-3` (шум MC) |
| замкнутая форма п.5 против кронекеровского решения | `2.6e-15` |
| `Pᵀ A P = I`, `Pᵀ C P = Λ` | `3.9e-15` |
| `‖F(X) − G‖ / ‖G‖` | `5.2e-15` |
| PSD: `min ⟨X, F(X)⟩` по 300 случайным `X` | `> 0` |
| дескент-лемма `⟨G, WX⟩ = ½⟨G_in, X⟩` | до 14 знака |
| знак: `dL` против `−½ηα·quad` | совпадает |
| ортогональность Cayley | `3.3e-16` |
| сохранение спектра `W` за 30 шагов | `σ ∈ [1.000000, 1.000000]` |
| сходимость на достижимой least-squares цели | `3.73 → 4.98e-4` за 300 шагов |

---

## Почему `S = I` — не уступка

Три попытки описать `S` простой моделью, все на обученном игрушечном
трансформере, относительная невязка `‖S − fit‖/‖S‖`:

| модель | невязка |
|---|---|
| `S ≈ βI` | `0.84 … 0.95` |
| `S ≈ αWAWᵀ + βI` | `0.82 … 0.94` |
| `S ≈ αVᵀS_next V + γVᵀV + βI` (`V` — нижележащий вес) | `0.72 … 0.84` |

То есть `S` анизотропна, и её анизотропия не сводится ни к изотропному шуму,
ни к части, индуцированной данными через `W`, ни к вкладу нижележащих весов.
Утверждать изотропность нельзя.

**Но близость `S` к единичной — не тот вопрос.** Важно, что происходит с шагом.
Замер: направление строится по половине данных, шаг применяется к одному слою,
угол ограничен `0.1` рад (там модель ещё точна до полупроцента), меряется
фактический лосс на отложенных батчах.

| слой | `ΔL` с измеренной `S` | `ΔL` с `S = I` | отношение |
|---|---|---|---|
| `b1.at.wv` | `−1.37e-03` | `−2.49e-03` | **1.82** |
| `b2.at.wv` | `−1.93e-03` | `−3.61e-03` | **1.87** |
| `b1.ff.gate` | `−3.20e-03` | `−4.57e-03` | **1.43** |
| `b2.ff.gate` | `−4.53e-03` | `−6.51e-03` | **1.44** |

`S = I` роняет лосс в **1.4–1.9 раза сильнее**, чем измеренная `S`. Механизм:
ковариация, оценённая по конечной выборке, переобучена, а её **обращение**
усиливает шум оценки — мы отдельно мерили, что шаг на 47% состоит из шума
выборки. Изотропизация здесь работает как регуляризация, а не как упрощение.

Это же объясняет, почему в литературе односторонний прекондиционер по входу
выигрывает у градиентного, а не просто ссылается на этот факт.

**Непроверено:** зависимость от размера выборки. `S` здесь оценена примерно по
80 тысячах токенов при `d_out = 256`. В реальном сеттинге EMA идёт по 131
тысяче токенов на шаг, выборка на порядки больше, и там измеренная `S` может
стать достаточно точной, чтобы её обращение окупалось. Прогон, который это
проверял, был убит до результата. **До проверки утверждение в статью нести
нельзя** — только как наблюдение на игрушечном масштабе.

## Обе стороны поворота нужны, и вклады складываются

Естественный вопрос: если `W ← R_out · W · R_in` — может, одна из сторон
ничего не даёт? Геометрия говорит, что нужны обе (орбита `{U Σ Vᵀ}` требует
двигать и левые, и правые сингулярные векторы), но это не значит, что вторая
приносит измеримую пользу.

Замер: шаг применяется к одному слою, каждая сторона со **своим** углом,
для варианта «обе» двумерный перебор 7×7, меряется фактический лосс на
отложенных батчах.

| слой | форма | in | out | обе | in/обе | out/обе | сумма |
|---|---|---|---|---|---|---|---|
| `b1.at.wq` | 256×256 | `-3.2e-4` | `-9.3e-4` | `-1.2e-3` | 0.27 | 0.77 | **1.04** |
| `b1.at.wv` | 256×256 | `-1.2e-3` | `-2.2e-3` | `-3.3e-3` | 0.36 | 0.68 | **1.04** |
| `b1.at.wo` | 256×256 | `-1.2e-3` | `-2.5e-3` | `-3.6e-3` | 0.33 | 0.69 | **1.02** |
| `b1.ff.gate` | 704×256 | `-2.1e-3` | `-3.4e-3` | `-5.2e-3` | 0.39 | 0.65 | **1.04** |
| `b1.ff.down` | 256×704 | `-9.6e-4` | `-2.9e-3` | `-3.8e-3` | 0.25 | 0.76 | **1.01** |
| `b3.at.wq` | 256×256 | `-1.0e-3` | `-2.1e-3` | `-3.0e-3` | 0.34 | 0.69 | **1.03** |
| `b3.at.wo` | 256×256 | `-2.1e-3` | `-2.3e-3` | `-4.2e-3` | 0.49 | 0.53 | **1.02** |
| `b3.ff.gate` | 704×256 | `-3.4e-3` | `-4.6e-3` | `-7.7e-3` | 0.45 | 0.60 | **1.05** |
| `b3.ff.down` | 256×704 | `-3.4e-3` | `-3.1e-3` | `-6.4e-3` | 0.54 | 0.49 | **1.03** |

**Сумма долей `1.01…1.05` во всех девяти слоях.** Стороны делают независимую,
неперекрывающуюся работу; избыточности, которую можно было бы срезать, нет.
Выбросив любую, теряешь её долю целиком — то есть примерно половину.

Гипотеза о частичном взаимопоглощении поворотов соседними слоями (выход `wv`
идёт прямо во вход `wo`, и без нелинейности повороты компенсировались бы) **не
подтвердилась**: тогда сумма была бы заметно больше единицы. `SiLU` ломает эту
симметрию достаточно, чтобы стороны стали независимы.

Асимметрия есть — out даёт 0.49–0.77, in 0.25–0.54 — но не такая, чтобы кого-то
выбрасывать. И у неё чёткий тренд по глубине: во всех четырёх сопоставимых
парах доля in-стороны в блоке 3 выше, чем в блоке 1.

**Побочно объясняется наблюдение самих авторов.** Их `alternate` против
`bilateral` стоит 0.23% (3.3654 против 3.3575). Аддитивность это объясняет:
раз стороны не взаимодействуют, разнесение их по разным шагам почти ничего не
стоит — оба результата про одно свойство, с разных сторон.

**Оговорка.** Оптимальный угол упёрся в `0.100` у всех вариантов — это верх
сетки, поставленный намеренно, потому что выше квадратичная модель теряет
точность. Числа означают «лучшее в пределах бюджета 0.1 рад». Упёрлись
одинаково все три варианта, так что относительное сравнение это не искажает.

## Экспериментальный сеттинг

Взят из `opt_llama_60M_pion.sh` в репозитории Pion, а не из статьи — в статье
часть чисел отсутствует, а часть противоречит скриптам.

| | |
|---|---|
| hidden / слоёв / голов | 512 / 8 / 8 |
| ffn hidden / kv channels | 1376 / 64 |
| нормализация | RMSNorm |
| seq_len | 256 |
| micro / global batch | 256 / 512 последовательностей |
| токенов на шаг | 131 072 |
| итераций | 73 242 (`= 9.6e9 / 512 / 256`) |
| lr | `1e-3`, косинус до `1e-5`, **без warmup** |
| weight decay / grad clip | `0.1` / `1.0` |
| инициализация | гауссова, `init_method_std = 0.02` |
| токенизатор / данные | T5-base / C4 |
| Pion применяется к | 2D-параметрам, кроме embedding и output |
| остальное | AdamW |

Бюджет токенов **разрешён**: их скрипт число шагов не задаёт, а выводит из
`TOKEN=9.6`, `GLOBAL_BATCH=512` и `TRAIN_ITER = TOTAL_TOKENS/GLOBAL_BATCH/256`.
Выходит 73 242 шага по 131 072 токена, то есть 9.6B — ровно 8× Chinchilla для
60M, что и означает их комментарий `# 1: 1.2; 2: 2.4; 4: 4.8; 8: 9.6;`.

## Осознанные отличия от бейзлайна

Три элемента взяты не как у Pion. Каждый — решение, а не упущение.

| | Pion | у нас | почему |
|---|---|---|---|
| момент | ambient, `β₁ = 0.9`, `β₂ = 0.95` | **нет** | чистый вывод без сглаживания; момент — кандидат на потом |
| масштабирование | `scaling rms`, `rms = 0.2` | **нет** | ~~проверяемая гипотеза: Фишер задаёт масштаб сам~~ — **гипотеза опровергнута 2026-08-27** тремя независимыми измерениями (`kappa = 1.8e-3`, `kfac/exact = 0.0128`, `alpha_exact/alpha` 5e-3…1e-1). `eta` — подбираемый learning rate. RMS-нормировки у нас по-прежнему нет, и цена этого — межслойный разброс угла 18–61× |
| ретракция | `exp` до `degree 2` | **Cayley** | точная ортогональность |

RMS у Pion устроен так: `alpha = lr · rms · √(mn) / ‖base‖_F`, то есть RMS
ambient-обновления на элемент равен ровно `lr · rms` независимо от градиента.
Величина шага полностью задана `lr`, генератор даёт только направление.

Про ретракцию есть измеренное основание. Усечённая экспонента ортогональна
лишь до `O(A³)`:

| угол | Cayley `‖RᵀR−I‖` | trunc2 `‖RᵀR−I‖` | trunc2 `‖R‖₂` |
|---|---|---|---|
| `0.1` | `1.6e-15` | `6.5e-06` | `1.0000` |
| `1.0` | `1.8e-15` | `6.5e-02` | `1.1180` |
| `5.0` | `7.8e-16` | `4.1e+01` | **`12.54`** |

За 2000 шагов при угле `0.05` относительный дрейф спектра: Cayley `7.1e-15`,
trunc2 `2.4e-04`. То есть заглавное свойство Pion — сохранение спектра —
у них приближённое, а с Cayley точное.

## Дизайн сравнения

Три отличия сразу означали бы три конфаунда, поэтому сравнивать надо не с
опубликованным Pion, а с **ablated Pion**: те же настройки, единственное
отличие — `F⁻¹`.

**Cayley в ablated-бейзлайне навязан, а не выбран.** Их усечённая экспонента
без RMS-нормировки расходится: замерено на случайных градиентах, `d = 24`,

| `lr` | none+trunc | none+cayley | rms+trunc | rms+cayley |
|---|---|---|---|---|
| `10⁻³` | `6.2e-07` | `6.4e-15` | `1.7e-11` | `6.4e-15` |
| `10⁻²` | `6.2e-03` | `3.3e-15` | `1.7e-07` | `9.7e-15` |
| `5·10⁻²` | **разошлось@40** | `6.3e-15` | `1.0e-04` | `6.2e-15` |
| `10⁻¹` | **разошлось@7** | `4.7e-15` | `1.7e-03` | `4.1e-15` |

RMS-нормировка у них несущая: она держит угол малым, и множитель `1.0078` за
шаг не компаундится. Со снятой нормировкой углы растут, и `1.0078⁵⁰⁰ ≈ 49`.
Значит абляция по scaling **невозможна** на их ретракции, и Cayley становится
её предусловием. Конфаунд исчезает.

| прогон | назначение |
|---|---|
| Pion, полный конфиг | контекст: опубликованный бейзлайн |
| Pion, ablated (none/none/Cayley) | **честный бейзлайн, одна переменная** |
| NGD-Pion | вклад кривизны = разница со второй строкой |

### Toy sanity check

Достижимая least-squares цель, `d = 32`, `cond(A) = 10³`, инициализация как у
них (`std = 0.02`), `lr` подобран по сетке из 28 значений для каждого метода:

| метод | `lr*` | лосс за 300 шагов | сокращение |
|---|---|---|---|
| Pion полный | `4.6e-03` | `2.24e-04` | 233× |
| Pion ablated | `2.2e+01` | `3.47e-04` | 151× |
| **NGD-Pion** | `1.0e+00` | **`3.17e-05`** | **1652×** |

Ablated-бейзлайн обучается — это и был вопрос, способный обнулить дизайн.

**Чего этот замер не показывает.** Цель квадратичная и точно достижимая,
шума нет, сид один. На квадратичной задаче с точной кривизной натуральный
градиент выигрывает почти тавтологически. Это необходимое условие, а не
достаточное: провал здесь означал бы остановку, успех не означает ничего
про реальный претрейн.



Три отличия сразу означают три конфаунда, поэтому сравнивать надо не с
опубликованным Pion, а с **ablated Pion**: те же три настройки (`momentum
none`, `scaling none`, Cayley), единственное отличие — `F⁻¹`.

| прогон | назначение |
|---|---|
| Pion, полный конфиг | контекст: опубликованный бейзлайн |
| Pion, ablated | **честный бейзлайн, одна переменная** |
| NGD-Pion | вклад кривизны = разница со второй строкой |

Первая строка нужна, чтобы ablated-версию нельзя было назвать соломенным
чучелом.

## Открыто

1. **Широкие слои (`d_out < d_in`) vs остальные.** Единственный случай
   с вырожденным пучком и единственный, где нужен `A^{-1/2}`. При `d_in > d_out` 76% энергии
   шага уходит на вращение игнорируемых входных направлений в рабочее
   подпространство. Математически корректно, полезность неизвестна.
   Вероятный источник расслоения результата.

2. **Momentum.** Split-half: шаг расходится на 47% (блок 0) между двумя
   независимыми выборками по 225k токенов. Фишер эту дисперсию не
   убирает — он её перевешивает, но не усредняет. Демпфирование лечит
   3-4%. Momentum лечил бы именно её. Главный кандидат в объяснение,
   если raw NGD-Pion окажется нестабильнее ванильного Pion.

3. **`T_fac`.** Инструмент есть — `α_t`. Вместо сетки по `T_fac`
   пересчитывать по порогу (`α_t < 0.9` → refactor), тогда каждый слой
   подстраивается сам.

4. **Бейзлайн.** Если vanilla Pion пойдёт с momentum, а NGD-Pion без,
   сравнение меряет две переменные.
