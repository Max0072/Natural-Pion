# NGD-Pion — алгоритм в чистом виде

Преемник `natural_gradient_pion.md`. Здесь зафиксировано то, что осталось
после проверки выкладок: все решения приняты, все тождества проверены
численно. Аппроксимаций нет — плотный точный путь `O(d³)`.

Addendum (sketch, CG, Woodbury, msign) в этом документе не участвует.

---

## 0. Геометрия

Pion обновляет `W ∈ R^{d_out×d_in}` двусторонним поворотом
`W ← R_out · W · R_in`, обе `R` ортогональны. Сингулярные числа `W`
не меняются никогда — двигаются только сингулярные векторы. Обучаемое
содержимое живёт в алгебре Ли `so(d)`, то есть в кососимметричных матрицах.

Следствие: спектр инициализации = спектр финальной модели, навсегда.

---

## 1. Генераторы

```
G_in  = Wᵀ G − Gᵀ W          (d_in × d_in,  кососимметричная)
G_out = G Wᵀ − W Gᵀ          (d_out × d_out, кососимметричная)
```

Это в точности удвоенный риманов градиент по повороту:

```
⟨G, W·X_in⟩ = ½⟨G_in, X_in⟩       для любой кососимметричной X_in
⟨G, X_out·W⟩ = ½⟨G_out, X_out⟩
```

Для одного сэмпла `G = δxᵀ` генератор — бивектор `G_in = δ' ∧ x`,
где `δ' = Wᵀδ`. Ранг 2. Батч из `B` токенов даёт ранг ≤ `2B`.

---

## 2. Оператор Фишера

Ковариация бивектора при `x ⊥ δ'` даёт оператор на кососимметричных `X`:

```
F(X) = 2 (B X C + C X B)
```

**Решение: `S = E[δδᵀ] := I`** (изотропные backward-ошибки). Тогда

```
in-сторона:   B = A,  C = S'   = Wᵀ W
out-сторона:  B = I,  C = A'   = W A Wᵀ
```

где `A = E[xxᵀ]` — единственная статистика, которую нужно копить.

`F` самосопряжён и PSD на кососимметричном подпространстве.

---

## 3. Статистика (каждый шаг)

```
A ← β A + (1−β) xxᵀ / n            β = 0.95
```

Только forward-активации. **Backward-хуки не нужны.**
Точность накопления: **fp32 минимум** (bf16 даёт ошибку в 10³–10⁴ раз).

---

## 4. Факторизация (раз в T_fac шагов)

**in-сторона** — конгруэнция пучка `(A, S')` через симметричный корень:

```
A = U Σ Uᵀ                              (eigh)
Σ ← max(Σ, ε · λ_max(A))                ПОЛ на спектр
A^{-1/2} = U Σ^{-1/2} Uᵀ
S' ← пол на спектр S' тем же ε
M   = A^{-1/2} S' A^{-1/2}
M   = Q Λ_in Qᵀ                         (eigh)
P_in = A^{-1/2} Q
```

даёт `P_inᵀ A P_in = I`, `P_inᵀ S' P_in = Λ_in`.

**out-сторона** — при `S = I` конгруэнция не нужна вовсе:

```
A' = W A Wᵀ = Q_out Λ_out Q_outᵀ        (eigh, P_out ортогональна)
```

Cholesky не используется: конгруэнции нужен любой множитель `B = KKᵀ`,
а симметричный корень позволяет явно контролировать малые собственные.

### Когда конгруэнция не нужна вовсе

`W ᵀW` зависит от формы. При semi-orthogonal инициализации:

| форма | `WᵀW` | in-сторона |
|---|---|---|
| `d_out > d_in` (высокая) | `= I` точно | обычный `eigh(A)`, базис ортогонален |
| `d_out = d_in` (квадрат) | `= I` точно | обычный `eigh(A)` |
| `d_out < d_in` (широкая) | проектор ранга `d_out` | нужен `A^{-1/2}` |

Для квадратной ортогональной `W` дополнительно: `A' = W A Wᵀ` — это `A`,
сопряжённая поворотом, поэтому `Λ_out = Λ_in`, а `P_out = W · P_in`.
**Один `eigh` обслуживает обе стороны**, второе разложение не нужно.

В блоке LLaMA (`d = 512`, `d_ff = 1408`):

| матрица | форма | `Λ_in` | `Λ_out` | `A^{-1/2}` | вырожд. сторона |
|---|---|---|---|---|---|
| `wq wk wv wo` | 512×512 квадрат | спектр `A` | спектр `A` | нет | нет |
| `ffn gate`, `ffn up` | 1408×512 высокая | спектр `A` | спектр `A` + нули | нет | **out** |
| `ffn down` | 512×1408 широкая | смесь `A` и `W` | сжатие `A` | **да** | **in** |

Два независимых свойства, которые легко спутать:

- **обращение** нужно только широким: там `WᵀW` — проектор, и конгруэнцию
  приходится анкорить на `A`.
- **вырожденность** есть у всех неквадратных, просто на разных сторонах:
  у широких `WᵀW` имеет ранг `d_out`, у высоких `W A Wᵀ` имеет размер
  `d_out` при ранге `d_in`.

Оба закрываются одним и тем же полом на спектр, см. ниже.

Градиент в мёртвый блок не попадает ни на одной стороне: на in- потому что
`δ' = Wᵀδ ⊥ null(W)`, на out- потому что `G_out = δ ∧ (Wx)` и `Wx ∈ range(W)`.
Измерено на высокой: `‖G̃‖` в блоке `(ноль,ноль)` равна `3.3e-08` против
`6.0e+01` по всей матрице.

**Главное следствие:** для шести матриц из семи `Λ` — это спектр `A`, и
отдельного объекта «оператор Фишера» в реализации не существует вовсе.
Его собственные значения суть `2(λᵢ + λⱼ)` по спектру `A`. Отсюда же следует,
почему демпфер нужен ровно один: и обращение, и знаменатель работают с одним
и тем же спектром, так что достаточно отфлорить его один раз.

### Демпфирование: одна ручка на три спектра

Вырождаться могут три матрицы — `A`, `WᵀW`, `W A Wᵀ` — каждая тогда, когда
данные или форма `W` не дают ей полного ранга. `W` не демпфируется нигде:
она входит только через две последние, а демпфер в самой `W` сломал бы
ортогональность, на которой всё держится.

**Правило одно и применяется к спектру:**

```
λ ← max(λ, ε·λ_max)
```

Пол, а не сдвиг. Пол тождественен для всех собственных значений выше него,
поэтому хорошо определённые направления сохраняют точный масштаб; сдвиг
`M + εI` возмущает и их, платя точностью везде за защиту, нужную только
малому концу. На широкой `W` при `ε = 10⁻⁴` пол даёт `5.8e-9` финального
лосса против `7.7e-7` у сдвига — в 134 раза.

Отфлорив три спектра, больше ничего подпирать не нужно: `A` защищена и как
анкер (`A^{-1/2}`), и как диагонализуемая (`Λ_in`), а знаменатели
`2(Λᵢ + Λⱼ)` положительны автоматически. **Двух параметров `ε_A` и `ε_F`
не существует — есть один `ε`.**

### `ε` — предохранитель, а не гиперпараметр

Без ядра он не нужен вовсе: квадратная `W` при полноранговой `A` сходится
при `ε = 0` до `3.4e-14`. Ядро есть у любой неквадратной `W` — у широких на
in-стороне, у высоких на out- — и там мёртвый блок есть честный `0/0`,
дающий NaN при `ε = 0`.

| `ε` | квадрат | высокая | широкая |
|---|---|---|---|
| `0` | `3.4e-14` | **NaN** | **NaN** |
| `10⁻¹⁰` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁸` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁶` | `3.4e-14` | `5.5e-09` | `5.7e-09` |
| `10⁻⁴` | `3.4e-14` | `2.1e-08` | `5.8e-09` |
| `10⁻²` | `2.2e-03` | `3.7e-03` | `3.3e-03` |

Плато — четыре порядка. Подбирать нечего: поставить достаточно большим,
чтобы не было NaN, и достаточно малым, чтобы пол не срабатывал на живых
направлениях.

**Но плато измерено в fp64 и в fp32 не переносится.** Нижняя граница пола
задаётся рабочей точностью: ниже машинного эпсилон малый конец пучка
округляется в шум, и пол там ничего не значит. На широком слое при
`cond(A) = 10⁴`, fp32 против fp64:

| `ε` | `10⁻⁸` | `10⁻⁶` | `10⁻⁵` | `10⁻⁴` | `10⁻²` |
|---|---|---|---|---|---|
| ошибка | `2.2e-01` | `5.6e-03` | `5.5e-04` | `7.7e-05` | `2.1e-06` |

Машинный эпсилон fp32 равен `1.2e-7`, поэтому `ε = 10⁻⁸` там просто
бессмыслен. **Дефолт `10⁻⁴`** — он и выше разрешения fp32, и совпадает со
статистическим оптимумом из held-out замера. В fp64 годится всё до `10⁻¹⁰`.

Пол ставится на все спектры, попадающие в знаменатель: `A`, `WᵀW`, `W A Wᵀ`
и **итоговый `Λ` пучка**. Последнее не избыточно — при неквадратной `W` пучок
растягивает `Λ` на больше порядков, чем разрешает рабочий тип, и без пола на
результате малый конец обнуляется в fp32 несмотря на отфлоренные источники.

**Условие:** semi-orthogonal**Условие:** semi-orthogonal инициализация — и **она не выбрана**.

Спектр `W` замораживается Pion навсегда, поэтому инициализация задаёт его
на всё обучение, и ради выразительности спектр берётся нетривиальным. Тогда
`WᵀW ≠ I` нигде, ярлык не срабатывает, и in-сторона всюду идёт через
конгруэнцию. Раздел выше остаётся как описание того, что было бы при
ортогональной инициализации, и как обоснование, почему out-сторона дешёвая
независимо от неё (она дешёвая из-за `S = I`, а не из-за `W`).

Цена этого решения измерена и оказалась мала:

| спектр `W` | `cond(W)` | `cond(Λ)` | ошибка fp32 | `‖X‖₂` |
|---|---|---|---|---|
| все единицы | `1.0` | `1.0e6` | `9.9e-3` | `20.8` |
| равномерно `[0.5, 2]` | `4.0` | `2.2e6` | `9.8e-3` | `14.4` |
| логравн. `[0.25, 4]` | `15.2` | `1.4e7` | `9.0e-3` | `13.8` |
| гауссова | `650` | `9.2e9` | `9.4e-3` | `22.2` |

Обусловленность пучка растёт на три порядка, но **ни точность, ни норма
шага от этого не зависят**: там, где `W` мала, мал и `δ' = Wᵀδ`, так что
экстремальные `Λ` встречаются с экстремально малым числителем. Ошибка fp32
определяется `cond(A)`, а не `cond(W)`.

Остаётся единственная цена — факторизация на in-стороне дороже в `4.3` раза
(`138` мс против `32` при `d = 512`). Амортизированная по `T_fac`, она
близка к проценту.

Отдельно стоит знать: у **квадратной** гауссовой матрицы `cond(W) ≈ d`
(плотность Марченко–Пастура не отделена от нуля при соотношении сторон 1),
а у прямоугольных с соотношением 2.75 — всего около 4. То есть если спектр
всё же захочется контролировать, трогать нужно матрицы внимания, а не FFN.

---

## 5. Натуральный градиент (каждый шаг)

```
Ỹ    = Pᵀ G_skew P
Y_ij = Ỹ_ij / (2(Λ_i + Λ_j))
X    = skew(P Y Pᵀ)
```

Демпфирования здесь нет. `Λ` приходит из уже отфлоренных спектров (§4),
поэтому знаменатели положительны по построению.

## 6. Trust-region

```
quad = ⟨G_in, X_in⟩ + ⟨G_out, X_out⟩
curv = ⟨X_in, F_in(X_in)⟩ + ⟨X_out, F_out(X_out)⟩
α_t  = min(α_max, quad / (curv + ε))      α_max = 1
```

При свежем базисе `α_t ≡ 1` тождественно: `X = F⁺G` даёт `F(X) = G`,
значит `curv = ⟨X, G⟩ = quad`. Это не приближение, а алгебра.

Отклоняется `α_t` только от **устаревания базиса**. Измерено на
достижимой least-squares цели: монотонный спад `1.000 → 0.996 → 0.992
→ 0.987 → 0.983 → 0.979` по шагам после пересчёта, минимум `0.469`
при `T_fac = 50`.

`α_max = 1` делает механизм односторонним: устаревший базис может
только укоротить шаг. При `α_max = 10` наблюдался разгон до `1.23`.

**Это единственный бесплатный сигнал обратной связи по `T_fac`.**

---

## 7. Обновление весов

```
W ← Cayley(−ηα_t X_out) · W · Cayley(−ηα_t X_in)

Cayley(−cX) = (I + c/2 · X)⁻¹ (I − c/2 · X)
```

Минус в шаге, `X` из п.5 без знака. Тогда
`ΔL ≈ −½ η α_t · quad ≤ 0` по лемме п.1.

Cayley точно ортогонален для кососимметричной `X` при любом вещественном
`c`; `I + cX/2` не вырождается никогда (чисто мнимые собственные значения).
Совпадает с `exp` до `z²`, расходится на `z³` — ретракция второго порядка.

---

## Гиперпараметры

| параметр | значение | статус |
|---|---|---|
| `η` | подбирать | единственный настоящий гиперпараметр |
| `ε` | `10⁻⁴` | предохранитель; нижняя граница задана точностью fp32 |
| `β` (EMA для `A`) | `0.95` | не проверялся |
| `α_max` | `1` | односторонний, обосновано |
| `T_fac` | `100`, фиксированный | адаптивный ломает воспроизводимость |
| точность шага | fp32 | fp64 ничего не даёт и недоступен на части бэкендов |
| точность `A` | fp32 минимум | bf16 даёт ошибку в `10³`–`10⁴` раз |

## Реализация

Модуль на раздел, чтобы код читался против этого документа:

| модуль | раздел | содержимое |
|---|---|---|
| `linalg.py` | — | `skew`, пол на спектр, Cayley |
| `covariance.py` | §3 | накопление `A`, с запретом на низкую точность |
| `factorization.py` | §4 | базисы, оба пути, батчинг по формам |
| `direction.py` | §1, §5, §6 | генераторы, решение, trust-region |
| `optimizer.py` | §7 | оркестрация и ретракция |
| `hooks.py` | — | адаптер под `nn.Linear` |
| `reference.py` | весь | numpy-транскрипция, оракул для тестов |

Оптимизатор принимает **параметры, а не модули**, и ковариация подаётся
снаружи. Это то, что позволяет существовать адаптерам под чужие типы слоёв —
`ColumnParallelLinear` в Megatron, слитый QKV — не трогая сам оптимизатор.

`reference.py` намеренно наивен: он транскрипция этого документа, а не рабочий
код. Torch-путь считается верным ровно настолько, насколько он его
воспроизводит, и это закреплено тестами.

## Решения и почему

| решение | почему |
|---|---|
| `S = I` | out-сторона схлопывается в ортогональный `eigh`, cond 9 против 7350; `A` — единственная статистика; KL-оптимально при фиксированном факторе (Lin et al. 2026); входная сторона эмпирически лучше градиентной (Eschenhagen et al. 2026) |
| симметричный корень, не Cholesky | конгруэнции хватает любого `B = KKᵀ`; малые собственные контролируются явно |
| маски нет | пол на спектрах делает знаменатели положительными; маска сверх этого не нужна |
| один `ε`, пол на три спектра | пол не трогает живые направления; на широкой `W` в 134 раза точнее сдвига; плато четыре порядка |

| никакого усечения | шаг живёт в малых собственных `A` (усиление `1/√λ`); усечение на `10⁻⁴` = ошибка 100% |
| `α_max = 1` | устаревший базис должен тормозить, а не разгонять |
| fp32 везде | на реальных спектрах сквозная ошибка `2.5e-5…1.7e-3` |
| без momentum | пока; см. открытые вопросы |

---

## Что проверено численно

| утверждение | невязка |
|---|---|
| `F(X) = 2(BXC + CXB)` против Монте-Карло (4M сэмплов) | `1.5e-3` (шум MC) |
| замкнутая форма п.5 против кронекеровского решения | `2.6e-15` |
| `Pᵀ A P = I`, `Pᵀ C P = Λ` | `3.9e-15` |
| `‖F(X) − G‖ / ‖G‖` | `5.2e-15` |
| PSD: `min ⟨X, F(X)⟩` по 300 случайным `X` | `> 0` |
| дескент-лемма `⟨G, WX⟩ = ½⟨G_in, X⟩` | до 14 знака |
| знак: `dL` против `−½ηα·quad` | совпадает |
| ортогональность Cayley | `3.3e-16` |
| сохранение спектра `W` за 30 шагов | `σ ∈ [1.000000, 1.000000]` |
| сходимость на достижимой least-squares цели | `3.73 → 4.98e-4` за 300 шагов |

---

## Экспериментальный сеттинг

Взят из `opt_llama_60M_pion.sh` в репозитории Pion, а не из статьи — в статье
часть чисел отсутствует, а часть противоречит скриптам.

| | |
|---|---|
| hidden / слоёв / голов | 512 / 8 / 8 |
| ffn hidden / kv channels | 1376 / 64 |
| нормализация | RMSNorm |
| seq_len | 256 |
| micro / global batch | 256 / 512 последовательностей |
| токенов на шаг | 131 072 |
| итераций | 37 500 |
| lr | `1e-3`, косинус до `1e-5`, **без warmup** |
| weight decay / grad clip | `0.1` / `1.0` |
| инициализация | гауссова, `init_method_std = 0.02` |
| токенизатор / данные | T5-base / C4 |
| Pion применяется к | 2D-параметрам, кроме embedding и output |
| остальное | AdamW |

**Нестыковка, требующая проверки:** `37500 × 131072 = 4.9B` токенов, тогда как
статья заявляет 9.6B. Одно из чисел неверно, и разница вдвое — это прямо
влияет на бюджет кластера.

## Осознанные отличия от бейзлайна

Три элемента взяты не как у Pion. Каждый — решение, а не упущение.

| | Pion | у нас | почему |
|---|---|---|---|
| момент | ambient, `β₁ = 0.9`, `β₂ = 0.95` | **нет** | чистый вывод без сглаживания; момент — кандидат на потом |
| масштабирование | `scaling rms`, `rms = 0.2` | **нет** | проверяемая гипотеза: Фишер задаёт масштаб сам |
| ретракция | `exp` до `degree 2` | **Cayley** | точная ортогональность |

RMS у Pion устроен так: `alpha = lr · rms · √(mn) / ‖base‖_F`, то есть RMS
ambient-обновления на элемент равен ровно `lr · rms` независимо от градиента.
Величина шага полностью задана `lr`, генератор даёт только направление.

Про ретракцию есть измеренное основание. Усечённая экспонента ортогональна
лишь до `O(A³)`:

| угол | Cayley `‖RᵀR−I‖` | trunc2 `‖RᵀR−I‖` | trunc2 `‖R‖₂` |
|---|---|---|---|
| `0.1` | `1.6e-15` | `6.5e-06` | `1.0000` |
| `1.0` | `1.8e-15` | `6.5e-02` | `1.1180` |
| `5.0` | `7.8e-16` | `4.1e+01` | **`12.54`** |

За 2000 шагов при угле `0.05` относительный дрейф спектра: Cayley `7.1e-15`,
trunc2 `2.4e-04`. То есть заглавное свойство Pion — сохранение спектра —
у них приближённое, а с Cayley точное.

## Дизайн сравнения

Три отличия сразу означают три конфаунда, поэтому сравнивать надо не с
опубликованным Pion, а с **ablated Pion**: те же три настройки (`momentum
none`, `scaling none`, Cayley), единственное отличие — `F⁻¹`.

| прогон | назначение |
|---|---|
| Pion, полный конфиг | контекст: опубликованный бейзлайн |
| Pion, ablated | **честный бейзлайн, одна переменная** |
| NGD-Pion | вклад кривизны = разница со второй строкой |

Первая строка нужна, чтобы ablated-версию нельзя было назвать соломенным
чучелом.

## Открыто

1. **Широкие слои (`d_out < d_in`) vs остальные.** Единственный случай
   с вырожденным пучком и единственный, где нужен `A^{-1/2}`. При `d_in > d_out` 76% энергии
   шага уходит на вращение игнорируемых входных направлений в рабочее
   подпространство. Математически корректно, полезность неизвестна.
   Вероятный источник расслоения результата.

2. **Momentum.** Split-half: шаг расходится на 47% (блок 0) между двумя
   независимыми выборками по 225k токенов. Фишер эту дисперсию не
   убирает — он её перевешивает, но не усредняет. Демпфирование лечит
   3-4%. Momentum лечил бы именно её. Главный кандидат в объяснение,
   если raw NGD-Pion окажется нестабильнее ванильного Pion.

3. **`T_fac`.** Инструмент есть — `α_t`. Вместо сетки по `T_fac`
   пересчитывать по порогу (`α_t < 0.9` → refactor), тогда каждый слой
   подстраивается сам.

4. **Бейзлайн.** Если vanilla Pion пойдёт с momentum, а NGD-Pion без,
   сравнение меряет две переменные.
